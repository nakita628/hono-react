{
  "name": "hono-react-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "deps": "rm -rf node_modules pnpm-lock.yaml && pnpm install --no-prefer-frozen-lockfile",
    "check": "vp check --fix"
  },
  "devDependencies": {
    "vite-plus": "^0.1.24"
  }
}
